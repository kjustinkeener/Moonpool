# Normalize line endings so cross-platform contributors get consistent diffs.
* text=auto eol=lf

# PowerShell scripts are Windows-facing and expect CRLF.
*.ps1 text eol=crlf

# Treat these as binary (no line-ending or diff munging).
*.png binary
*.ico binary
*.icns binary
*.AppImage binary
